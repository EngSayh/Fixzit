# ⚠️ إشعار أمني حرج | CRITICAL SECURITY NOTICE

**تاريخ:** 2025-10-09  
**الحالة:** 🔴 يتطلب إجراء فوري

---

## 🚨 مشكلة JWT Secret مكشوفة

تم اكتشاف أن ملف `lib/auth.ts` يحتوي على JWT secret ثابتة مكتوبة مباشرة في الكود (الأسطر 100 و 121).

### الخطورة
- **المستوى:** حرج جداً (CRITICAL)
- **التأثير:** اختراق كامل لنظام المصادقة
- **الاستغلال:** يمكن لأي شخص لديه وصول للكود تزوير التوكنات

---

## ✅ الإجراءات الفورية المطلوبة

### 1. إزالة المفتاح من الكود (فوراً)
```bash
# افتح lib/auth.ts وأزل القيمة الثابتة من الأسطر 100 و 121
```

### 2. تدوير المفتاح (إنشاء مفتاح جديد)
```bash
# إنشاء مفتاح جديد
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### 3. تخزين المفتاح في Secrets Manager
```bash
# AWS Secrets Manager
aws secretsmanager create-secret \
  --name fixzit-jwt-production \
  --secret-string '{"JWT_SECRET":"your-new-secret-here"}'

# أو في ملف .env للتطوير فقط (لا تدفعه إلى Git)
echo "JWT_SECRET=your-new-secret-here" >> .env.local
```

### 4. إزالة المفتاح من تاريخ Git
```bash
# استخدم git-filter-repo (الطريقة الموصى بها)
git filter-repo --path lib/auth.ts --invert-paths

# أو استخدم BFG Repo-Cleaner
# bfg --delete-files auth.ts
```

### 5. فرض إعادة تسجيل الدخول لجميع المستخدمين
- إبطال جميع التوكنات الحالية
- إعلام المستخدمين بالحاجة لإعادة تسجيل الدخول
- مراقبة محاولات الوصول غير المصرح به

---

## 📋 قائمة التحقق

- [ ] إزالة المفتاح من lib/auth.ts
- [ ] إنشاء مفتاح JWT جديد
- [ ] تخزين المفتاح في AWS Secrets Manager
- [ ] تحديث متغيرات البيئة في CI/CD
- [ ] إزالة المفتاح من تاريخ Git
- [ ] إعادة بناء ونشر التطبيق
- [ ] إبطال جميع التوكنات الحالية
- [ ] إعلام المستخدمين
- [ ] مراجعة logs للوصول المشبوه
- [ ] تحديث التوثيق الأمني

---

## 🔒 الوقاية المستقبلية

### 1. استخدام Pre-commit Hooks
```bash
# تثبيت detect-secrets
pip install detect-secrets

# إضافة hook
cat > .git/hooks/pre-commit << 'EOF'
#!/bin/bash
detect-secrets scan --baseline .secrets.baseline
EOF

chmod +x .git/hooks/pre-commit
```

### 2. Code Review Checklist
- ✅ عدم وجود أسرار في الكود
- ✅ استخدام متغيرات بيئة فقط
- ✅ جميع الأسرار في Secrets Manager
- ✅ عدم كتابة أسرار حقيقية في التقارير

### 3. Automated Scanning
- تفعيل GitHub Secret Scanning
- استخدام GitGuardian أو Snyk
- فحص منتظم للمستودع

---

## 📞 جهات الاتصال

في حالة اكتشاف أي نشاط مشبوه:
- Security Team: security@fixzit.com
- Emergency Hotline: [رقم الطوارئ]

---

## 📚 المراجع

- [AWS Secrets Manager Documentation](https://docs.aws.amazon.com/secretsmanager/)
- [OWASP Secret Management Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_CheatSheet.html)
- [git-filter-repo](https://github.com/newren/git-filter-repo)

---

**آخر تحديث:** 2025-10-09  
**المسؤول:** Security Team  
**الحالة:** في انتظار التنفيذ
