# AI Memory - Batch files are regenerated locally
# Only commit the master-index.json and outputs (processed summaries)

# Batch files are too large and regenerated by smart-chunker.js
batches/

# Keep outputs directory structure
!outputs/.gitkeep
