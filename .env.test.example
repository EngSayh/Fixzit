# =============================================================================
# TEST ENVIRONMENT VARIABLES - OTP AUTHENTICATION
# =============================================================================
# Copy this file to .env.test and fill in actual test account phone numbers
# .env.test is gitignored for security
# =============================================================================

# === SUPER ADMIN TEST ACCOUNT ===
TEST_SUPERADMIN_PHONE=+966500000001

# === ADMIN TEST ACCOUNT ===
TEST_ADMIN_PHONE=+966500000002

# === MANAGER TEST ACCOUNT ===
TEST_MANAGER_PHONE=+966500000003

# === TECHNICIAN TEST ACCOUNT ===
TEST_TECHNICIAN_PHONE=+966500000004

# === TENANT TEST ACCOUNT ===
TEST_TENANT_PHONE=+966500000005

# === VENDOR TEST ACCOUNT ===
TEST_VENDOR_PHONE=+966500000006

# =============================================================================
# AUTHENTICATION (Required for E2E tests)
# =============================================================================
# NextAuth configuration for Playwright tests
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=test-secret-key-for-playwright-do-not-use-in-production

# Google OAuth (Optional - prevents warning logs in tests)
# Use test/development credentials, NOT production
# Get from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-test-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-test-client-secret

# =============================================================================
# AV SCAN STATUS TOKENS (PER-ORG) FOR TESTS
# =============================================================================
# Optional: set when testing token-based scan-status polling (without session cookies)
# Example map: {"tenant-1":"test-scan-token"}
SCAN_STATUS_TOKENS_BY_ORG={"tenant-1":"test-scan-token"}
# Alternatively set single org+token pair:
SCAN_STATUS_TOKEN_ORG=tenant-1
SCAN_STATUS_TOKEN=
SCAN_STATUS_TOKEN_REQUIRED=false

# =============================================================================
# NOTES:
# - These accounts must exist in your test database with these phone numbers
# - Phone numbers must be in E.164 format (+966XXXXXXXXX)
# - OTP codes are returned in test mode (NODE_ENV=test or development)
# - For CI/CD, set these as environment variables
# - Never commit .env.test to version control
# - GOOGLE credentials are optional but recommended to remove warning logs
# =============================================================================
