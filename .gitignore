# Dependencies & builds
/node_modules/
/.next/
/dist/
/out/
/build/
/coverage/
/playwright-report/
/playwright-artifacts/
tests/playwright-artifacts/
/playwright/.cache/
/test-results/
/tests/playwright-artifacts/
# Playwright auth state (contains session tokens - never commit)
tests/state/*.json
/cypress/videos/
/cypress/screenshots/
tsconfig.tsbuildinfo
*.tsbuildinfo

# Local env & secrets
.env
.env.*
!.env.example
!.env.test.example
.env.local.bak
*.pem
*.key
*.crt

# Dev login credentials (never commit real credentials)
/app/dev/login-helpers/credentials.ts
/dev/credentials.local.ts

# OS/editor noise
.DS_Store
Thumbs.db
*.swp

# Archives & reports & backups
*.zip
*.tar
*.tar.gz
*.tgz
*.gz
*.bak
*.bak3
*.backup
*.tmp
*.log
*.final
*.final3
*.emergency-bak
duplicate-detection-report.json

# Duplicate files (created by OS when copying)
*\ (1)*
*\ (2)*
*\ (3)*

# Temp directories (prevent tracking temporary files)
/tmp/
/.tmp/

# Local artifacts we'll keep but not commit
/_archive/
/.archive/
/_artifacts/
!/_artifacts/eslint-baseline.json
!/_artifacts/eslint-any-scan.json
!/_artifacts/eslint-scripts-tools-scan.json
/tests/_artifacts/
/qa/artifacts/sidebar-*.png
/reports/
/exports/
/screenshots/
/.archived-data/
/docs/archive/
/docs/reports/archive/
/docs/sessions/archive/

# Jupyter notebooks (use docs/notebooks/ for committed ones)
Untitled*.ipynb

# Runtime files
server.pid
*.pid

# Hosting/platform
.vercel
.netlify
node_modules

# Ignore Next build outputs
.next/
.env.production.test
playwright-report/
/tmp/

# Translation audit artifacts (regenerated by pre-commit hook)
docs/translations/translation-audit.json
docs/translations/translation-audit.csv

# Package manager policy: This project uses pnpm exclusively
# package-lock.json is intentionally excluded to prevent npm/pnpm conflicts
# Dependency locking is handled by pnpm-lock.yaml
package-lock.json
