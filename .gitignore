# Dependencies & builds
/node_modules/
/.next/
/dist/
/out/
/build/
/coverage/
/playwright-report/
/playwright-artifacts/
/playwright/.cache/
/test-results/
/cypress/videos/
/cypress/screenshots/
tsconfig.tsbuildinfo
*.tsbuildinfo

# Local env & secrets
.env
.env.*
!.env.example
!.env.test.example
.env.local.bak
*.pem
*.key
*.crt

# Dev login credentials (never commit)
/app/dev/login-helpers/credentials.ts
/dev/credentials.server.ts

# OS/editor noise
.DS_Store
Thumbs.db
*.swp

# Archives & reports & backups
*.zip
*.tar
*.tar.gz
*.tgz
*.gz
*.bak
*.backup
*.tmp
*.log

# Temp directory (prevent tracking large patch files)
/tmp/

# Local artifacts we'll keep but not commit
/_archive/
/_artifacts/
/reports/
/exports/
/screenshots/
/.archived-data/
/docs/archive/
/docs/reports/archive/
/docs/sessions/archive/

# Jupyter notebooks (use docs/notebooks/ for committed ones)
Untitled*.ipynb

# Runtime files
server.pid
*.pid

# Hosting/platform
.vercel
.netlify
node_modules

# Ignore Next build outputs
.next/
.env.production.test
playwright-report/
/tmp/

# Translation audit artifacts (regenerated by pre-commit hook)
docs/translations/translation-audit.json
docs/translations/translation-audit.csv

# Package manager policy: This project uses pnpm exclusively
# package-lock.json is intentionally excluded to prevent npm/pnpm conflicts
# Dependency locking is handled by pnpm-lock.yaml
# package-lock.json
